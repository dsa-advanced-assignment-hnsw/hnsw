# HNSW Semantic Search Backend Requirements
# Compatible with both pip and conda environments
# Use this file for creating new environments or installing dependencies

# Web Framework
flask==3.0.0
flask-cors==4.0.0
Werkzeug==3.0.1

# Machine Learning & AI
torch>=2.0.0
torchvision>=0.15.0
git+https://github.com/openai/CLIP.git

# Paper Search - Sentence Transformers
sentence-transformers>=2.2.0

# Vector Search
hnswlib>=0.8.0

# Data Handling
numpy>=1.24.0,<2.0.0
h5py>=3.8.0
Pillow>=9.5.0

# Document Processing (for paper search)
PyPDF2>=3.0.0

# HTTP Client (for fetching images from online sources)
requests>=2.31.0

# Utilities
tqdm>=4.65.0

# Production Server (optional)
gunicorn>=21.0.0

# Visualization Server (FastAPI)
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# Note: If you're using conda, you may want to install some packages via conda first:
# conda install -c conda-forge numpy h5py pillow requests tqdm
# Then install the remaining packages with: pip install -r requirements.txt
